{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE"
  },
  "services": {
    "backend": {
      "name": "Backend",
      "buildCommand": "npm ci && cd backend && npx prisma generate && npx prisma db push && npm run build",
      "startCommand": "cd backend && npm run start"
    }
  }
}
