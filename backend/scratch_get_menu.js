const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const items = await prisma.menuItem.findMany({ select: { name: true, category: true, price: true, isAvailable: true }, orderBy: { category: 'asc' } }); console.log('Found ' + items.length + ' items in the menu:'); console.table(items.map(i => ({ Name: i.name, Category: i.category, Price: i.price, Available: i.isAvailable ? 'Yes' : 'No' }))); } finally { await prisma.$disconnect(); } } main();
